export interface ShoppingListItem {
    id: string
    name: string //(name of the ingredient)
    quantity: string //(quantity needed for the recipe)
    checked: boolean //(whether the item is checked off)
}
